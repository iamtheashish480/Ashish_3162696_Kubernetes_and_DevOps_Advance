apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
data:
  DB_HOST: "postgres-service" # Connecting internally using Kubernetes CoreDNS name routing hooks
  DB_NAME: "company_db"
  DB_USER: "postgres_admin"
